@model IEnumerable<Team_Project_4.Models.Hoadon>

@{
    ViewData["Title"] = "Hóa Đơn";
    string userRole = Context.Session.GetString("UserRole");
    if (userRole == "Manager")
    {
       Layout = "_ManagerLayout";
    }
    else if(userRole == "Staff")
    {
        Layout = "_StaffLayout";
    }
}

<style>
    .centered-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: vh;
    }

    .right-aligned {
        text-align: right;
    }

    .form-group-label {
        margin-bottom: 3px; /* Thêm khoảng cách giữa các nhóm label-input */
    }
</style>

<script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>

<h1 class="text-center mb-3">LỊCH SỬ HÓA ĐƠN</h1>
<div class="mb-3"></div>
<div>
    <form asp-action="Test2">
        <button type="submit" class="btn btn-primary float-right">Lập Hóa Đơn</button>
    </form>
</div>

<div class="mb-3"></div>
<table class="table table-striped table-bordered text-center">
    <thead class="thead-dark">
        <tr>
            <th>
                Tên Khách Hàng
            </th>
            <th>
                Tên Phòng
            </th>
            <th>
                Ngày Thanh Toán
            </th>
            <th>
                Tổng Tiền
            </th>
            <th>Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Tenkh)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tenphong)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ngaylaphd)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tongtien)
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Mahd" class="btn btn-info">Chi tiết</a>
                    <form asp-action="Delete" asp-route-id="@item.Mahd" method="post" class="d-inline"
                          onsubmit="return confirm('Xác nhận xóa hóa đơn này?');">
                        <button type="submit" class="btn btn-danger">Xóa</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.js"></script>
<script>
    jQuery(document).ready(function ($) {
        var table = $('.table').DataTable({
            paging: false, // Disable paging
            searching: false // Disable searching
        });

        // Apply sorting to all columns by default (you can specify columns for sorting if needed)
        table.order([2, 'desc']).draw();
    });
</script>
